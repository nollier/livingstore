<h1>Liste des produits du devis <%= @devi.nom %> - <%= @devi.numero %> </h1>

<table>
  <tr>
    <th>Fabricant</th>
    <th>Name</th>
    <th>Element</th>
    <th>Category</th>
    <th>Revetement</th>
    <th>Price</th>
    <th>Height</th>
    <th>Width</th>
    <th>Deep</th>
    <th>Devi</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @produits.each do |produit| %>
  <tr>
    <td><%= produit.fabricant %></td>
    <td><%= produit.name %></td>
    <td><%= produit.element %></td>
    <td><%= produit.category %></td>
    <td><%= produit.revetement %></td>
    <td><%= produit.price %></td>
    <td><%= produit.height %></td>
    <td><%= produit.width %></td>
    <td><%= produit.deep %></td>
    <td><%= link_to 'Détail', [produit.devi, produit] %></td>
    <td><%= link_to 'Modifier', edit_devi_produit_path(produit.devi, produit) %></td>
    <td><%= link_to 'Supprimer', [produit.devi, produit], method: :delete, data: { confirm: 'Dernier mot?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nouveau produit', new_devi_produit_path(params[:devi_id]) %>
<%= link_to 'Retour', devi_path(params[:devi_id]) %>
